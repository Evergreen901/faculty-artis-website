import React from 'react';
import socials from '../mocks/socialmedia.json';

export const SocialMediaLinks = () => {
  return (
    <>
      {/* {socials.data.map((item, index) => {
        return (
          <li key={index}>
            <a
              target={'_blank'}
              href={item.url}
              title={item.title}
              rel='noreferrer'
            >
              {item.title}
            </a>
          </li>
        );
      })} */}
    </>
  );
};
